# Member5 工作进度

## 当前状态

初步完成组员 5 负责的业务逻辑与权限系统任务，当前已经具备可运行的后端接口和可访问的前端页面。

## 已完成内容

1. RBAC 与登录鉴权初版
   - 接入 Sa-Token 登录态。
   - 登录接口改为基于数据库用户表校验。
   - 补充角色与权限列表返回，管理员具备写权限。

2. 农资供应商管理
   - 完成 `agri_material_supplier` 的分页、查询、新增、修改、删除后端接口。
   - 完成前端供应商管理页面，支持关键字和合作状态筛选。

3. 传感器管理
   - 完成 `sensor` 的分页、查询、新增、修改、删除后端接口。
   - 完成传感器在线状态接口化查询。
   - 完成前端传感器管理页面，展示设备状态和最新采集数据。

4. 农事日志与产量统计扩展
   - 完成 `farm_log` 农事日志后端接口和前端页面。
   - 完成 `yield_stat` 产量统计后端接口和前端页面。
   - 完成日志分类统计和计划产量汇总查询。

5. 前端页面与项目结构
   - 将前端由占位页改为 Vue Router 多页面应用。
   - 新增首页、成员5总览、供应商、传感器、农事日志、产量统计页面。
   - 清理本地拼写错误的旧后端目录 `packages/servrer`，统一使用 `packages/server`。

## 后续需要完善

1. 数据大屏仍需继续完善。
   - 当前成员5页面以业务管理和表格展示为主。
   - 后续需要结合 ECharts 或可视化组件制作更完整的数据大屏，包括供应商状态分布、设备在线率、农事操作趋势、产量趋势等。

2. 前端交互仍可增强。
   - 当前新增、编辑、删除按钮已作为入口展示，后续可继续补弹窗表单和真实提交流程。
   - 可继续补分页器、批量操作、表单校验和删除确认。

3. 数据库初始化需要保持一致。
   - 当前本地数据库密码配置为 `123456`。
   - 其他组员运行时需按本机 MySQL 密码调整 `application.yml`。

## 验证情况

- 后端编译通过：`mvn -q -DskipTests compile`
- 前端构建通过：`npm run build`
- 本地启动验证通过：后端 `8080`，前端 `5173`
