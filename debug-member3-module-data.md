# Debug Session: member3-module-data

Status: [OPEN]

## Goal
修改前端主页模块分支名称，补齐五个模块入口；对齐成员3模块接口与后端数据，解决打开成员3模块时报错和无法准确展示数据的问题。

## Hypotheses
1. 前端主页或路由仍使用“成员1/成员3/成员5”等旧名称，导致模块入口语义和路径不一致。
2. 成员3前端 API 路径与后端 IoT Controller 实际路径不一致，导致请求 404 或响应结构不匹配。
3. 前端请求封装的 baseURL、响应拦截或 token 处理与后端返回格式不一致，导致数据被错误解析。
4. 成员3页面字段名与后端 VO 字段名不一致，导致接口返回成功但页面渲染时报 undefined 或空数据。
5. 后端数据库已有 IoT 数据，但前端默认查询参数或路由初始化逻辑不符合后端接口要求，导致打开模块时报错。

## Evidence Log
待收集。
