# 模块骨架占位说明

本次补充仅用于预留后续开发结构，不调整现有业务主体。

## 前端预留

- `packages/web/src/api/`：后续接口封装入口
- `packages/web/src/components/`：后续通用组件目录
- `packages/web/src/composables/`：后续组合式逻辑目录

## 后端预留

- `packages/servrer/src/main/resources/mapper/`：后续 MyBatis Mapper XML 目录
- `packages/servrer/src/test/java/com/smartfarm/`：后续测试代码目录

## 备注

当前仅放置占位文件，保证仓库结构清晰且不影响现有功能。
